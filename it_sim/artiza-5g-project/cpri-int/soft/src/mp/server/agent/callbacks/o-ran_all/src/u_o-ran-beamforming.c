
/*
 * Copyright (c) 2008 - 2012, Andy Bierman, All Rights Reserved.
 * Copyright (c) 2012 - 2019, YumaWorks, Inc., All Rights Reserved.
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 *

*** Generated by yangdump-sdk 18.10-9

    User SIL-SA module
    bundle o-ran_all
    module o-ran-beamforming
    revision 2019-04-01
    namespace urn:o-ran:beamforming:1.0
    organization O-RAN Alliance
    Created: 2019-06-21T02:49:14Z
    CLI parameters:
        defnames true
        format uc
        indent 4
        module ietf-yang-types
        module ietf-inet-types
        module ietf-interfaces
        module iana-if-type
        module ietf-ip
        module iana-hardware
        module ietf-hardware
        module ietf-netconf-acm
        module o-ran-hardware
        module o-ran-software-management
        module o-ran-interfaces
        module o-ran-transceiver
        module o-ran-mplane-int
        module o-ran-dhcp
        module o-ran-compression-factors
        module o-ran-uplane-conf
        module o-ran-module-cap
        module o-ran-processing-element
        module o-ran-performance-management
        module o-ran-fm
        module o-ran-lbm
        module o-ran-supervision
        module o-ran-beamforming
        module o-ran-operations
        module o-ran-usermgmt
        module o-ran-sync
        sil-bundle o-ran_all
        sil-get2
        sil-sa
        unified true

 */

#include <xmlstring.h>

#include "procdefs.h"
#include "agt.h"
#include "agt_action.h"
#include "agt_cb.h"
#include "agt_not.h"
#include "agt_rpc.h"
#include "agt_sil_lib.h"
#include "agt_timer.h"
#include "agt_util.h"
#include "agt_val.h"
#include "cfg.h"
#include "dlq.h"
#include "getcb.h"
#include "ncx.h"
#include "ncx_feature.h"
#include "ncxmod.h"
#include "ncxtypes.h"
#include "obj.h"
#include "rpc.h"
#include "ses.h"
#include "sil_sa.h"
#include "status.h"
#include "val.h"
#include "val_child.h"
#include "val_util.h"
#include "xml_util.h"
#include "xml_val.h"

/* SIL bundle: o-ran_all */
#include "y_ietf-yang-types.h"
#include "u_ietf-yang-types.h"
#include "y_ietf-inet-types.h"
#include "u_ietf-inet-types.h"
#include "y_ietf-interfaces.h"
#include "u_ietf-interfaces.h"
#include "y_iana-if-type.h"
#include "u_iana-if-type.h"
#include "y_ietf-ip.h"
#include "u_ietf-ip.h"
#include "y_iana-hardware.h"
#include "u_iana-hardware.h"
#include "y_ietf-hardware.h"
#include "u_ietf-hardware.h"
#include "y_ietf-netconf-acm.h"
#include "u_ietf-netconf-acm.h"
#include "y_o-ran-hardware.h"
#include "u_o-ran-hardware.h"
#include "y_o-ran-software-management.h"
#include "u_o-ran-software-management.h"
#include "y_o-ran-interfaces.h"
#include "u_o-ran-interfaces.h"
#include "y_o-ran-transceiver.h"
#include "u_o-ran-transceiver.h"
#include "y_o-ran-mplane-int.h"
#include "u_o-ran-mplane-int.h"
#include "y_o-ran-dhcp.h"
#include "u_o-ran-dhcp.h"
#include "y_o-ran-compression-factors.h"
#include "u_o-ran-compression-factors.h"
#include "y_o-ran-uplane-conf.h"
#include "u_o-ran-uplane-conf.h"
#include "y_o-ran-module-cap.h"
#include "u_o-ran-module-cap.h"
#include "y_o-ran-processing-element.h"
#include "u_o-ran-processing-element.h"
#include "y_o-ran-performance-management.h"
#include "u_o-ran-performance-management.h"
#include "y_o-ran-fm.h"
#include "u_o-ran-fm.h"
#include "y_o-ran-lbm.h"
#include "u_o-ran-lbm.h"
#include "y_o-ran-supervision.h"
#include "u_o-ran-supervision.h"
#include "y_o-ran-beamforming.h"
#include "u_o-ran-beamforming.h"
#include "y_o-ran-operations.h"
#include "u_o-ran-operations.h"
#include "y_o-ran-usermgmt.h"
#include "u_o-ran-usermgmt.h"
#include "y_o-ran-sync.h"
#include "u_o-ran-sync.h"

#include "i_o-ran-beamforming.h"
#include "f_saa_com_init.h"

static obj_template_t *beamforming_information_update_obj;

/* put your static variables here */
static char o_ran_beamforming_type[]                        = i_para_o_ran_beamforming_type;
static char o_ran_beamforming_frequency_max_num_beam_id[]   = i_para_o_ran_beamforming_frequency_max_num_beam_id;
static char o_ran_beamforming_frequency_initial_beam_id[]   = i_para_o_ran_beamforming_frequency_initial_beam_id;
static char o_ran_beamforming_frequency_compression_type[]  = i_para_o_ran_beamforming_frequency_compression_type;
static char o_ran_beamforming_frequency_bitwidth[]          = i_para_o_ran_beamforming_frequency_bitwidth;
static char o_ran_beamforming_frequency_exponent[]          = i_para_o_ran_beamforming_frequency_exponent;
static char o_ran_beamforming_time_max_num_beam_id[]        = i_para_o_ran_beamforming_time_max_num_beam_id;
static char o_ran_beamforming_time_initial_beam_id[]        = i_para_o_ran_beamforming_time_initial_beam_id;
static char o_ran_beamforming_time_frequency_granularity[]  = i_para_o_ran_beamforming_time_frequency_granularity;
static char o_ran_beamforming_time_time_granularity[]       = i_para_o_ran_beamforming_time_time_granularity;
static char o_ran_beamforming_time_compression_type[]       = i_para_o_ran_beamforming_time_compression_type;
static char o_ran_beamforming_time_bitwidth[]               = i_para_o_ran_beamforming_time_bitwidth;
static char o_ran_beamforming_time_exponent[]               = i_para_o_ran_beamforming_time_exponent;
static uint16 o_ran_beamforming_properties_beam_id=0;
/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_compression_format_get
*
* Get database object callback for choice compression-format
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/frequency/frequency-domain-beams/compression-format
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_compression_format_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* get the active case; determined by the SIL or SIL-SA
     * callback based on instances in the system
     * It may be NULL if no active case and choice is optional */
    const xmlChar *active_case_modname = NULL;
    const xmlChar *active_case = y_o_ran_beamforming_N_block_floating_point;

    if (active_case == NULL) {
        return ERR_NCX_NO_INSTANCE;
    }

    /* set the active case return values */
    res = getcb_set_active_case(get2cb, active_case_modname, active_case);
    if (res != NO_ERR) {
        if(LOGWARN){
            log_warn("\n%s getcb_set_active_case is fail.", __func__);
        }
        return res;
    }

    /* get the template for the active case */
    obj_template_t *case_obj = 
        obj_find_child(obj, active_case_modname, active_case);
    if (case_obj == NULL) {
        if(LOGWARN){
            log_warn("\n%s case_obj is not found.", __func__);
        }
        return ERR_NCX_NOT_FOUND;
    }
    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, case_obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */
        xmlChar *para_temp;
        if (!xml_strcmp(active_case, y_o_ran_beamforming_N_block_floating_point)) {
            if (!xml_strcmp(name, y_o_ran_beamforming_N_exponent)) {
                /* leaf exponent (uint8) */
                res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_frequency_exponent, &para_temp);
                val_value_t *return_val = agt_make_leaf2(
                    case_obj,
                    active_case_modname,
                    y_o_ran_beamforming_N_exponent,
                    (const xmlChar *)para_temp,
                    &res);
                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_compression_format_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_get
*
* Get database object callback for container frequency-domain-beams
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/frequency/frequency-domain-beams
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_max_number_of_beam_ids)) {
            /* leaf max-number-of-beam-ids (uint16) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_frequency_max_num_beam_id, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_max_number_of_beam_ids);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_max_number_of_beam_ids,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_initial_beam_id)) {
            /* leaf initial-beam-id (uint16) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_frequency_initial_beam_id, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_initial_beam_id);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_initial_beam_id,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_compression_type)) {
            /* leaf compression-type (enumeration) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_frequency_compression_type, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_compression_type);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_compression_type,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_bitwidth)) {
            /* leaf bitwidth (uint8) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_frequency_bitwidth, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_bitwidth);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_bitwidth,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_frequency_frequency_domain_beams_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_compression_format_get
*
* Get database object callback for choice compression-format
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/time/time-domain-beams/compression-format
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_compression_format_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */


    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* get the active case; determined by the SIL or SIL-SA
     * callback based on instances in the system
     * It may be NULL if no active case and choice is optional */
    const xmlChar *active_case_modname = NULL;
    const xmlChar *active_case = y_o_ran_beamforming_N_block_floating_point;

    if (active_case == NULL) {
        return ERR_NCX_NO_INSTANCE;
    }

    /* set the active case return values */
    res = getcb_set_active_case(get2cb, active_case_modname, active_case);
    if (res != NO_ERR) {
        if(LOGWARN){
            log_warn("\n%s getcb_set_active_case is fail.", __func__);
        }
        return res;
    }

    /* get the template for the active case */
    obj_template_t *case_obj = 
        obj_find_child(obj, active_case_modname, active_case);
    if (case_obj == NULL) {
        if(LOGWARN){
            log_warn("\n%s case_obj is not found.", __func__);
        }
        return ERR_NCX_NOT_FOUND;
    }
    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, case_obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */
        xmlChar *para_temp;
        if (!xml_strcmp(active_case, y_o_ran_beamforming_N_block_floating_point)) {
            if (!xml_strcmp(name, y_o_ran_beamforming_N_exponent)) {
                /* leaf exponent (uint8) */
                res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_exponent, &para_temp);
                if(res != NO_ERR)
                {
                    if(LOGWARN){
                        log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_exponent);
                    }
                    return res;
                }
                val_value_t *return_val = agt_make_leaf2(
                    case_obj,
                    active_case_modname,
                    y_o_ran_beamforming_N_exponent,
                    (const xmlChar *)para_temp,
                    &res);
                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_compression_format_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_get
*
* Get database object callback for container time-domain-beams
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/time/time-domain-beams
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_max_number_of_beam_ids)) {
            /* leaf max-number-of-beam-ids (uint16) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_max_num_beam_id, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_max_number_of_beam_ids);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_max_number_of_beam_ids,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_initial_beam_id)) {
            /* leaf initial-beam-id (uint16) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_initial_beam_id, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_initial_beam_id);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_initial_beam_id,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_frequency_granularity)) {
            /* leaf frequency-granularity (enumeration) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_frequency_granularity, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_frequency_granularity);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_frequency_granularity,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_time_granularity)) {
            /* leaf time-granularity (enumeration) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_time_granularity, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_time_granularity);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_time_granularity,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_compression_type)) {
            /* leaf compression-type (enumeration) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_compression_type, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_compression_type);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_compression_type,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_bitwidth)) {
            /* leaf bitwidth (uint8) */
            res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_time_bitwidth, &para_temp);
            if(res != NO_ERR)
            {
                if(LOGWARN){
                    log_warn("\n%s %s is NULL.", __func__, y_o_ran_beamforming_N_bitwidth);
                }
                return res;
            }
            /* add beamforming-type to get2cb return_valQ */
            val_value_t *return_val = agt_make_leaf2(obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_bitwidth,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_time_time_domain_beams_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_compression_format_get
*
* Get database object callback for choice compression-format
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/hybrid/hybrid-beams/compression-format
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_compression_format_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* get the active case; determined by the SIL or SIL-SA
     * callback based on instances in the system
     * It may be NULL if no active case and choice is optional */
    const xmlChar *active_case_modname = NULL;
    const xmlChar *active_case = NULL;

    if (active_case == NULL) {
        return ERR_NCX_NO_INSTANCE;
    }

    /* set the active case return values */
    res = getcb_set_active_case(get2cb, active_case_modname, active_case);
    if (res != NO_ERR) {
        return res;
    }

    /* get the template for the active case */
    obj_template_t *case_obj = 
        obj_find_child(obj, active_case_modname, active_case);
    if (case_obj == NULL) {
        return ERR_NCX_NOT_FOUND;
    }
    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, case_obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);
        (void)name; /* remove yumaworks warning */

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_compression_format_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_get
*
* Get database object callback for container hybrid-beams
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type/hybrid/hybrid-beams
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        if (!xml_strcmp(name, y_o_ran_beamforming_N_max_number_of_beam_ids)) {
            /* leaf max-number-of-beam-ids (uint16) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_initial_beam_id)) {
            /* leaf initial-beam-id (uint16) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_frequency_granularity)) {
            /* leaf frequency-granularity (enumeration) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_time_granularity)) {
            /* leaf time-granularity (enumeration) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_compression_type)) {
            /* leaf compression-type (enumeration) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_bitwidth)) {
            /* leaf bitwidth (uint8) */

        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_hybrid_hybrid_beams_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_get
*
* Get database object callback for choice beamforming-type
* Path: /beamforming-config/per-band-config/static-properties/beamforming-type
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    (void)k_beamforming_config_per_band_config_band_number; /* remove yumaworks warning */

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* get the active case; determined by the SIL or SIL-SA
     * callback based on instances in the system
     * It may be NULL if no active case and choice is optional */

    xmlChar *beamforming_type;
    res = i_o_ran_beamforming_saa_init_data_get(o_ran_beamforming_type, &beamforming_type);
    if(res != NO_ERR)
    {
        if(LOGWARN){
            log_warn("\n%s beamforming_type is NULL.", __func__);
        }
        return res;
    }

    /* beamforming_typeが"frequency" or "time"以外の場合はNG応答 */
    if((xml_strcmp(beamforming_type, y_o_ran_beamforming_N_frequency)) && (xml_strcmp(beamforming_type, y_o_ran_beamforming_N_time)))
    {
        if(LOGWARN){
            log_warn("\n%s Unacceptable beamforming_type：%s.", __func__, beamforming_type);
        }
        res = ERR_NCX_OPERATION_FAILED;
        return res;
    }

    const xmlChar *active_case_modname = NULL;
    const xmlChar *active_case = (const xmlChar *)beamforming_type;

    if (active_case == NULL) {
        return ERR_NCX_NO_INSTANCE;
    }

    /* set the active case return values */
    res = getcb_set_active_case(get2cb, active_case_modname, active_case);
    if (res != NO_ERR) {
        if(LOGWARN){
            log_warn("\n%s getcb_set_active_case is fail.", __func__);
        }
        return res;
    }

    /* get the template for the active case */
    obj_template_t *case_obj = 
        obj_find_child(obj, active_case_modname, active_case);
    if (case_obj == NULL) {
        if(LOGWARN){
            log_warn("\n%s case_obj is not found.", __func__);
        }
        return ERR_NCX_NOT_FOUND;
    }
    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, case_obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);
        (void)name; /* remove yumaworks warning */

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_beamforming_type_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_get
*
* Get database object callback for container static-properties
* Path: /beamforming-config/per-band-config/static-properties
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("\nEnter %s: key_name=%s", __func__, k_beamforming_config_per_band_config_band_number);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_rt_bf_weights_update_support)) {
            /* leaf rt-bf-weights-update-support (boolean) */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_static_properties_get(y_o_ran_beamforming_N_rt_bf_weights_update_support, &para_temp);

            if( res == NO_ERR )
            {
                /* add beamforming-type to get2cb return_valQ */
                val_value_t *return_val = agt_make_leaf2(obj,
                    y_o_ran_beamforming_M_o_ran_beamforming,
                    y_o_ran_beamforming_N_rt_bf_weights_update_support,
                    (const xmlChar *)para_temp,
                    &res);
                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
            else
            {
                if(LOGWARN)
                {
                    log_warn("\n%s:%d operation failed. para=%s", __func__, __LINE__, y_o_ran_beamforming_N_rt_bf_weights_update_support);
                }
                return res;
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_number_of_beams)) {
            /* leaf number-of-beams (uint16) */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_static_properties_get(y_o_ran_beamforming_N_number_of_beams, &para_temp);
            if(res == NO_ERR)
            {
                /* add beamforming-type to get2cb return_valQ */
                val_value_t *return_val = agt_make_leaf2(obj,
                    y_o_ran_beamforming_M_o_ran_beamforming,
                    y_o_ran_beamforming_N_number_of_beams,
                    (const xmlChar *)para_temp,
                    &res);

                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
            else
            {
                if(LOGWARN)
                {
                    log_warn("\n%s:%d para=%s", __func__, __LINE__, y_o_ran_beamforming_N_number_of_beams);
                }
                return res;
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_static_properties_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get
*
* Get database object callback for container beamforming-property
* Path: /beamforming-config/per-band-config/beam-information/beamforming-properties/beamforming-property
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number,
    uint16 k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id)
{
    if (LOGINFO) {
        log_info("\nEnter %s beam-id=%d, key_name=%s", __func__, k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id,k_beamforming_config_per_band_config_band_number);
    }

    /* For GETNEXT, set the more_data flag true if not sure */
    boolean         more_data = TRUE;
    unsigned long   max_num_of_use = 0;
    xmlChar         *max_num_para;
    status_t        res = NO_ERR;
    char            *endptr;
    res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get_para( &max_num_para );
    if( res == NO_ERR )
    {
        max_num_of_use = strtoul((char *)max_num_para, &endptr, 10);
    }
    else
    {
        return res;
    }

    if(max_num_of_use == k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id)
    {
        more_data = FALSE;
    }

    /**** SET more_data FLAG ****/
    GETCB_GET2_MORE_DATA(get2cb) = more_data;

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    uint16 beam_id = 0;
    if(k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id == 0)
    {
        if(LOGDEV1)
        {
            log_dev1("\n%s:Call to calback with xget.", __func__); 
        }
        beam_id = o_ran_beamforming_properties_beam_id;
        o_ran_beamforming_properties_beam_id++;
        if(o_ran_beamforming_properties_beam_id >= max_num_of_use)
        {
            o_ran_beamforming_properties_beam_id = 0;
        }
    }
    else
    {
        if(LOGDEV1)
        {
            log_dev1("\n%s:Call to calback with sget.", __func__); 
        }
        beam_id = (uint16)(k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id - 1);
    }
    if(LOGDEV1)
    {
        log_dev1("\n%s:beam_id=%u.", __func__, beam_id); 
    }

    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_beam_type)) {
            /* leaf beam-type (enumeration) */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get_para(beam_id,
                y_o_ran_beamforming_N_beam_type,
                &para_temp);
            if( res == NO_ERR )
            {
                /* add coarse-fine to get2cb return_valQ */
                val_value_t *return_val = agt_make_leaf2(obj,
                    y_o_ran_beamforming_M_o_ran_beamforming,
                    y_o_ran_beamforming_N_beam_type,
                    (const xmlChar *)para_temp,
                    &res);
                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
            else
            {
                if(LOGWARN)
                {
                    log_warn( "\n%s:%d Failed to get param, beamforming_properties para=%s", __func__, __LINE__, y_o_ran_beamforming_N_beam_type );
                }
                return res;
            }
            if(LOGDEV1)
            {
                log_dev1("\n%s:%d", __func__, __LINE__); 
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_beam_group_id)) {
            /* leaf beam-group-id (uint16) */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get_para(beam_id,
                y_o_ran_beamforming_N_beam_group_id,
                &para_temp);
            if(res == NO_ERR)
            {
                /* add beam-groups-id to get2cb return_valQ */
                val_value_t *return_val = agt_make_leaf2(obj,
                    y_o_ran_beamforming_M_o_ran_beamforming,
                    y_o_ran_beamforming_N_beam_group_id,
                    (const xmlChar *)para_temp,
                    &res);
                if (return_val) {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
            else
            {
                if( LOGWARN )
                {
                    log_warn("\n%s:%d Failed to get param, para = %s", __func__, __LINE__, y_o_ran_beamforming_N_beam_group_id);
                }
                return res;
            }
            if(LOGDEV1)
            {
                log_dev1("\n%s:%d", __func__, __LINE__); 
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_coarse_fine_beam_relation)) {
            /* leaf-list coarse-fine-beam-relation (leafref) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_neighbour_beams)) {
            /* leaf-list neighbour-beams (leafref) */
            unsigned int list_counter = 0;
            res = NO_ERR;
            while(res == NO_ERR)
            {
                res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get_para2(beam_id,
                    list_counter,
                    y_o_ran_beamforming_N_neighbour_beams,
                    &para_temp);
                if(res == NO_ERR)
                {
                    /* add coarse-fine-beam-relation to get2cb return_valQ */
                    val_value_t *return_val = agt_make_leaf2(obj,
                        y_o_ran_beamforming_M_o_ran_beamforming,
                        y_o_ran_beamforming_N_neighbour_beams,
                        (const xmlChar *)para_temp,
                        &res);
                    if (return_val) {
                        getcb_add_return_val(get2cb, return_val);
                    }
                }
                list_counter++;
            }
            if (res == ERR_NCX_OPERATION_FAILED)
            {
                res = NO_ERR;
                if(LOGDEV1)
                {
                    log_dev1("\n%s:%d", __func__, __LINE__); 
                }
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_beamforming_property_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get
*
* Get database object callback for list beamforming-properties
* Path: /beamforming-config/per-band-config/beam-information/beamforming-properties
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number,
    uint16 k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id,
    boolean beam_id_fixed,
    boolean beam_id_present)
{
    boolean getnext = FALSE;
    char            beam_id_para[D_PARAM_NUM_MAX];
    unsigned long   max_num_of_use = 0;
    char            *endptr;

    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
        log_info("\n    present=%d ,fixed=%d", beam_id_present, beam_id_fixed);
        log_info("\n    key_beam_id=%hd", k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id);
        log_info("\n    key_name=%s", k_beamforming_config_per_band_config_band_number);
    }

    snprintf(beam_id_para, D_PARAM_NUM_MAX, "%d", k_beamforming_config_per_band_config_beam_information_beamforming_properties_beam_id );

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        getnext = TRUE;
        break;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    uint32 max_entries = GETCB_GET2_MAX_ENTRIES(get2cb);
    (void)max_entries; /* remove yumaworks warning */


    /* For GET, find the entry that matches the key values
     * For GETNEXT, find the entry that matches the next key value
     * If the 'present' flag is false then return first key instance
     * If the 'fixed' flag is true then no GETNEXT advance for the key
     * Create a new return key val_value_t, then getcb_add_return_key */

    xmlChar *max_num_para;
    res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get_para( &max_num_para );
    if( res == NO_ERR )
    {
        max_num_of_use = strtoul((char *)max_num_para, &endptr, 10);
        if( max_num_of_use == 0 )
        {
            return ERR_NCX_NO_INSTANCE;
        }
    }
    else
    {
        return res;
    }

    /* For GETNEXT, set the more_data flag true if not sure */
    boolean more_data = TRUE;

    /* key object */
    const xmlChar *key_name = 0;
    xmlChar *key_name_temp = NULL;

    /* key number check */
    uint32  curindex = 0;
    boolean name_ok = FALSE;
    if (beam_id_present)
    {
        /* get key's number from key */
        res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get_key_num(
            &curindex,
            (uint32)max_num_of_use,
            (const xmlChar *)beam_id_para);
        if(res == NO_ERR)
        {
            name_ok = TRUE;
        }
        else
        {
            return ERR_NCX_NO_INSTANCE;
        }
    }

    /* check validity of keys present */
    if (getnext) {
        if (beam_id_fixed) {
            return ERR_NCX_NO_INSTANCE;
        }

        /* adjust the key to find the next entry after
         * the specified value
         */
        if (!beam_id_present)
        {
            // return first key [0]
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get_first_key(&key_name_temp);
            if (res != NO_ERR)
            {
                return res;
            }
        }
        else
        {
            curindex++;
            /* find the correct key to retrieve */
            if ((res == NO_ERR) && (curindex < (uint32)max_num_of_use))
            {
                res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get_target_key(curindex, &key_name_temp);
                if (res == NO_ERR)
                {
                    /* check if the current entry is the last one
                     * and set more_data flag accordingly
                     */
                    if (curindex == ((uint32)max_num_of_use - 1))
                    {
                        more_data = FALSE;
                    }
                }
                else
                {
                    return ERR_NCX_NO_INSTANCE;
                }
                if(LOGDEV1)
                {
                    log_dev1("\n %s:%d key_name=%s", __func__, __LINE__, key_name_temp);
                    log_dev1("\n %s:%d more data=%d", __func__, __LINE__, more_data);
                }
            }
            else
            {
                return ERR_NCX_NO_INSTANCE;
            }
        }
    }
    else {
        if (beam_id_present && name_ok)
        {
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get_target_key(curindex, &key_name_temp);
            if(res != NO_ERR)
            {
                return ERR_NCX_NO_INSTANCE;
            }
        }
        else
        {
            /* get the first interface name */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get_first_key(&key_name_temp);
            if(LOGDEV1)
            {
                log_dev1("\n %s:%d key_name=%s", __func__, __LINE__, key_name_temp);
                log_dev1("\n %s:%d more data=%d", __func__, __LINE__, more_data);
            }
            if (res != NO_ERR)
            {
                return res;
            }
        }
    }

    if (key_name_temp == NULL)
    {
        return NO_ERR;
    }

    key_name = (const xmlChar *)key_name_temp;

    /* if we are here, then the index is valid */
    obj_template_t *name_obj = obj_find_child(obj, y_o_ran_beamforming_M_o_ran_beamforming, y_o_ran_beamforming_N_beam_id);
    if(name_obj == NULL)
    {
        /* error log */
        if(LOGWARN) {
            log_warn("\n%s: name_obj is NULL", __func__);
        }
        return ERR_NCX_NO_INSTANCE;
    }

    val_value_t *retval = agt_make_leaf2(name_obj, y_o_ran_beamforming_M_o_ran_beamforming, y_o_ran_beamforming_N_beam_id, key_name, &res);
    if (retval)
    {
        if (beam_id_fixed)
        {
            VAL_SET_FIXED_VALUE(retval);
        }
        getcb_add_return_key(get2cb, retval);
    }


    /***** ADD RETURN KEYS AND REMOVE THIS COMMENT ****/

    //if (GETCB_GET2_FIRST_RETURN_KEY(get2cb) == NULL) {
    //    return ERR_NCX_NO_INSTANCE;
    //}

    /**** SET more_data FLAG ****/

    GETCB_GET2_MORE_DATA(get2cb) = more_data;

    /* no terminal child nodes to check */

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_beamforming_properties_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get
*
* Get database object callback for container beam-information
* Path: /beamforming-config/per-band-config/beam-information
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number)
{
    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
        log_info("\n    key_name=%s", k_beamforming_config_per_band_config_band_number);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_number_of_beamforming_properties))
        {
            /* leaf number-of-beamforming-properties (uint16) */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get_para(&para_temp);
            if(res == NO_ERR)
            {
                val_value_t *return_val = agt_make_leaf2(
                    obj,
                    y_o_ran_beamforming_M_o_ran_beamforming,
                    y_o_ran_beamforming_N_number_of_beamforming_properties,
                    (const xmlChar *)para_temp,
                    &res );
                if (return_val)
                {
                    getcb_add_return_val(get2cb, return_val);
                }
            }
            else
            {
                if(LOGWARN)
                {
                    log_warn("\n%s: Failed to get param, para=%s", __func__, y_o_ran_beamforming_N_max_number_of_ues);
                }
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_beam_information_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_per_band_config_get
*
* Get database object callback for list per-band-config
* Path: /beamforming-config/per-band-config
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_per_band_config_get (
    getcb_get2_t *get2cb,
    const xmlChar *k_beamforming_config_per_band_config_band_number,
    boolean band_number_fixed,
    boolean band_number_present)
{
    boolean getnext = FALSE;

    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
        log_info("\n    present=%d ,fixed=%d", band_number_present, band_number_fixed);
        log_info("\n    key_name=%s", k_beamforming_config_per_band_config_band_number);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        getnext = TRUE;
        break;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    uint32 max_entries = GETCB_GET2_MAX_ENTRIES(get2cb);


    /* For GET, find the entry that matches the key values
     * For GETNEXT, find the entry that matches the next key value
     * If the 'present' flag is false then return first key instance
     * If the 'fixed' flag is true then no GETNEXT advance for the key
     * Create a new return key val_value_t, then getcb_add_return_key */

    /***** ADD RETURN KEYS AND REMOVE THIS COMMENT ****/

    //if (GETCB_GET2_FIRST_RETURN_KEY(get2cb) == NULL) {
    //    return ERR_NCX_NO_INSTANCE;
    //}

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* For GETNEXT, set the more_data flag true if not sure */
    boolean more_data = TRUE;

    /* key object */
    const xmlChar *key_name = 0;
    xmlChar *key_name_temp = NULL;

    /* key number check */
    uint32 curindex = 0;
    boolean name_ok = FALSE;
    if (band_number_present) {
        /* get key's number from key */
        res = i_o_ran_beamforming_beamforming_config_per_band_config_get_key_num(&curindex,
            saa_beamforming_max_band_num,
            k_beamforming_config_per_band_config_band_number);
        if(res == NO_ERR)
        {
            name_ok = TRUE;
        }
        else
        {
            return ERR_NCX_NO_INSTANCE;
        }
    }

    /* check validity of keys present */
    if (getnext) {
        if (band_number_fixed) {
            return ERR_NCX_NO_INSTANCE;
        }

        /* adjust the key to find the next entry after
         * the specified value
         */
        if (!band_number_present) {
            // return first key [0]
            res = i_o_ran_beamforming_beamforming_config_per_band_config_get_first_key(&key_name_temp);
            if (res != NO_ERR) {
                return res;
            }
        }
        else {
            curindex++;
            /* find the correct key to retrieve */
            if ((res == NO_ERR) && (curindex < max_entries))
            {
                res = i_o_ran_beamforming_beamforming_config_per_band_config_get_target_key(curindex, &key_name_temp);
                if (res == NO_ERR)
                {
                    /* check if the current entry is the last one
                     * and set more_data flag accordingly
                     */
                    if (curindex == (max_entries - 1))
                    {
                        more_data = FALSE;
                    }
                }
                else
                {
                    return ERR_NCX_NO_INSTANCE;
                }
            }
            else {
                return ERR_NCX_NO_INSTANCE;
            }
        }
    }
    else {
        if (band_number_present && name_ok)
        {
            res = i_o_ran_beamforming_beamforming_config_per_band_config_get_target_key(curindex, &key_name_temp);
            if(res != NO_ERR) {
                return ERR_NCX_NO_INSTANCE;
            }
        }
        else
        {
            /* get the first interface name */
            res = i_o_ran_beamforming_beamforming_config_per_band_config_get_first_key(&key_name_temp);
            if (res != NO_ERR) {
                return res;
            }
        }
    }

    if (key_name_temp == NULL) {
        return NO_ERR;
    }

    key_name = (const xmlChar *)key_name_temp;

    /* if we are here, then the index is valid */
    obj_template_t *name_obj = obj_find_child(obj, y_o_ran_beamforming_M_o_ran_beamforming, y_o_ran_beamforming_N_band_number);
    if(name_obj == NULL) {
        /* error log */
        if(LOGWARN) {
            log_warn("\n%s: name_obj is NULL", __func__);
        }
        return ERR_NCX_NO_INSTANCE;
    }

    val_value_t *retval = agt_make_leaf2(name_obj, y_o_ran_beamforming_M_o_ran_beamforming, y_o_ran_beamforming_N_band_number, key_name, &res);
    if (retval) {
        if (band_number_fixed) {
            VAL_SET_FIXED_VALUE(retval);
        }
        getcb_add_return_key(get2cb, retval);
    }

    if (LOGDEV1) {
        log_dev1("\n%s: more_data=%d", __func__, more_data);
    }

    /**** SET more_data FLAG ****/

    GETCB_GET2_MORE_DATA(get2cb) = more_data;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        xmlChar *para_temp;
        if (!xml_strcmp(name, y_o_ran_beamforming_N_tx_array)) {
            /* leaf-list tx-array (leafref) */
            for(uint32 ant_num = 0; ant_num < saa_beamforming_max_ant_num; ant_num++)
            {
                res = i_o_ran_beamforming_beamforming_config_per_band_config_get_para(curindex, ant_num, y_o_ran_beamforming_N_tx_array, &para_temp);
                if (LOGDEV1) {
                    log_dev1("\n%s: ant_num = %d, para_temp=%s", __func__, ant_num, para_temp);
                }
                if(res == NO_ERR)
                {
                    val_value_t *return_val = agt_make_leaf2(obj,
                        y_o_ran_beamforming_M_o_ran_beamforming,
                        y_o_ran_beamforming_N_tx_array,
                        (const xmlChar *)para_temp,
                        &res);
                    if (return_val) {
                        getcb_add_return_val(get2cb, return_val);
                    }
                }
                else
                {
                    return res;
                }
            }
        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_rx_array)) {
            /* leaf-list rx-array (leafref) */
                        
            for(uint32 ant_num = 0; ant_num < saa_beamforming_max_ant_num; ant_num++)
            {
                res = i_o_ran_beamforming_beamforming_config_per_band_config_get_para(curindex, ant_num, y_o_ran_beamforming_N_rx_array, &para_temp);
                if (LOGDEV1) {
                    log_dev1("\n%s: ant_num = %d, para_temp=%s", __func__, ant_num, para_temp);
                }
                if(res == NO_ERR)
                {
                    val_value_t *return_val = agt_make_leaf2(obj,
                        y_o_ran_beamforming_M_o_ran_beamforming,
                        y_o_ran_beamforming_N_rx_array,
                        (const xmlChar *)para_temp,
                        &res);
                    if (return_val)
                    {
                        getcb_add_return_val(get2cb, return_val);
                    }
                }
                else
                {
                    return res;
                }
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_per_band_config_get */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_ue_specific_beamforming_get
*
* Get database object callback for container ue-specific-beamforming
* Path: /beamforming-config/ue-specific-beamforming
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_ue_specific_beamforming_get (
    getcb_get2_t *get2cb)
{
    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* check if the requested node exists; determined by the
     * SIL or SIL-SA callback based on instances in the system  */
    boolean node_exists = TRUE;

    if (!node_exists) {
        return ERR_NCX_NO_INSTANCE;
    }

    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        if (!xml_strcmp(name, y_o_ran_beamforming_N_max_number_of_ues)) {
            /* leaf max-number-of-ues (uint8) */
            xmlChar c_para_temp[] = "0";
            xmlChar * para_temp = (xmlChar *)&c_para_temp;
            val_value_t *return_val = agt_make_leaf2(
                obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_max_number_of_ues,
                (const xmlChar *)para_temp,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }
        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_ue_specific_beamforming_get */


#ifdef u_o_ran_beamforming_F_MODIFY_BF_CONFIG
/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_operational_properties_get
*
* Get database object callback for container operational-properties
* Path: /beamforming-config/operational-properties
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_operational_properties_get (
    getcb_get2_t *get2cb)
{
    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        if (!xml_strcmp(name, y_o_ran_beamforming_N_number_of_writeable_beamforming_files)) {
            /* leaf number-of-writeable-beamforming-files (uint8) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_update_bf_non_delete)) {
            /* leaf update-bf-non-delete (boolean) */

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_persistent_bf_files)) {
            /* leaf persistent-bf-files (boolean) */

        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_operational_properties_get */

#endif /* u_o_ran_beamforming_F_MODIFY_BF_CONFIG */
/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_config_get
*
* Get database object callback for container beamforming-config
* Path: /beamforming-config
* Fill in 'get2cb' response fields
*
* INPUTS:
*     see ncx/getcb.h for details (getcb_fn2_t)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_beamforming_config_get (
    getcb_get2_t *get2cb)
{
    if (LOGINFO) {
        log_info("\nEnter %s:", __func__);
    }

    /* check the callback mode type */
    getcb_mode_t cbmode = GETCB_GET2_CBMODE(get2cb);
    switch (cbmode) {
    case GETCB_GET_VALUE:
        break;
    case GETCB_GETNEXT_VALUE:
        return ERR_NCX_NO_INSTANCE;
    default:
        return SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* an NP container always exists so no test for node_exists
     * by the SIL or SIL-SA callback is needed */
    obj_template_t *obj = GETCB_GET2_OBJ(get2cb);
    status_t res = NO_ERR;

    /* optional: check if any content-match nodes are present */
    boolean match_test_done = FALSE;
    val_value_t *match_val = GETCB_GET2_FIRST_MATCH(get2cb);
    for (; match_val; match_val =
        GETCB_GET2_NEXT_MATCH(get2cb, match_val)) {

        /**** CHECK CONTENT NODES AGAINST THIS ENTRY ***/

    }
    GETCB_GET2_MATCH_TEST_DONE(get2cb) = match_test_done;

    /* go through all the requested terminal child objects */
    obj_template_t *childobj =
        getcb_first_requested_child(get2cb, obj);
    for (; childobj; childobj =
        getcb_next_requested_child(get2cb, childobj)) {

        const xmlChar *name = obj_get_name(childobj);

        /* Retrieve the value of this terminal node and 
         * add with getcb_add_return_val */

        if (!xml_strcmp(name, y_o_ran_beamforming_N_beamforming_trough_attributes_supported)) {
            /* leaf beamforming-trough-attributes-supported (boolean) */
            boolean beam_att_sup = FALSE;

            /* add beamforming-trough-attributes-supported return_valQ */
            val_value_t *return_val = agt_make_boolean_leaf(
                obj,
                y_o_ran_beamforming_M_o_ran_beamforming,
                y_o_ran_beamforming_N_beamforming_trough_attributes_supported,
                beam_att_sup,
                &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }

        } else if (!xml_strcmp(name, y_o_ran_beamforming_N_beamforming_trough_ue_channel_info_supported)) {
            /* leaf beamforming-trough-ue-channel-info-supported (boolean) */
            boolean beam_chl_inf = FALSE;

            /* add beamforming-trough-ue-channel-info-supported return_valQ */
            val_value_t *return_val = agt_make_boolean_leaf(obj,
                                                            y_o_ran_beamforming_M_o_ran_beamforming,
                                                            y_o_ran_beamforming_N_beamforming_trough_ue_channel_info_supported,
                                                            beam_chl_inf,
                                                            &res);
            if (return_val) {
                getcb_add_return_val(get2cb, return_val);
            }

        }
    }

    return res;

} /* u_o_ran_beamforming_beamforming_config_get */


#ifdef u_o_ran_beamforming_F_MODIFY_BF_CONFIG
/********************************************************************
* FUNCTION u_o_ran_beamforming_activate_beamforming_config_validate
*
* RPC validation phase
* All YANG constraints have passed at this point.
* Add description-stmt checks in this function.
*
* INPUTS:
*     see agt/agt_rpc.h for details
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_activate_beamforming_config_validate (
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode)
{
    status_t res = NO_ERR;
    val_value_t *errorval = NULL;

    if (LOGDEBUG) {
        log_debug("\nStart SIL validate rpc <activate-beamforming-config> from module o-ran-beamforming");
    }

    val_value_t *inputval = agt_get_rpc_input(msg);
    if (inputval == NULL) return ERR_NCX_OPERATION_FAILED;

    val_value_t *v_beamforming_config_file_val = NULL;
    const xmlChar *v_beamforming_config_file;

    val_value_t *v_band_number_val = NULL;
    const xmlChar *v_band_number;

    (void)v_beamforming_config_file; /* pgr0039 */ /* remove yumaworks warning */
    (void)v_band_number; /* pgr0039 */ /* remove yumaworks warning */

    v_beamforming_config_file_val = val_find_child(
        inputval,
        y_o_ran_beamforming_M_o_ran_beamforming,
        y_o_ran_beamforming_N_beamforming_config_file);
    if (v_beamforming_config_file_val) {
        v_beamforming_config_file = VAL_STRING(v_beamforming_config_file_val);
    }

    v_band_number_val = val_find_child(
        inputval,
        y_o_ran_beamforming_M_o_ran_beamforming,
        y_o_ran_beamforming_N_band_number);
    if (v_band_number_val) {
        v_band_number = VAL_STRING(v_band_number_val);
    }

    if (res != NO_ERR) {
        agt_record_error(
            scb,
            &msg->mhdr,
            NCX_LAYER_OPERATION,
            res,
            methnode,
            (errorval) ? NCX_NT_VAL : NCX_NT_NONE,
            errorval,
            (errorval) ? NCX_NT_VAL : NCX_NT_NONE,
            errorval);
    }
    return res;

} /* u_o_ran_beamforming_activate_beamforming_config_validate */

/********************************************************************
* FUNCTION u_o_ran_beamforming_activate_beamforming_config_invoke
*
* RPC invocation phase
* All constraints have passed at this point.
* Call device instrumentation code in this function.
*
* INPUTS:
*     see agt/agt_rpc.h for details
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_activate_beamforming_config_invoke (
    ses_cb_t *scb,
    rpc_msg_t *msg,
    xml_node_t *methnode)
{
    status_t res = NO_ERR;

    if (LOGDEBUG) {
        log_debug("\nStart SIL invoke rpc <activate-beamforming-config> from module o-ran-beamforming");
    }

    val_value_t *inputval = agt_get_rpc_input(msg);
    if (inputval == NULL) return ERR_NCX_OPERATION_FAILED;

    val_value_t *v_beamforming_config_file_val = NULL;
    const xmlChar *v_beamforming_config_file;

    val_value_t *v_band_number_val = NULL;
    const xmlChar *v_band_number;
    (void)v_beamforming_config_file; /* pgr0039 */ /* remove yumaworks warning */
    (void)v_band_number; /* pgr0039 */ /* remove yumaworks warning */

    v_beamforming_config_file_val = val_find_child(
        inputval,
        y_o_ran_beamforming_M_o_ran_beamforming,
        y_o_ran_beamforming_N_beamforming_config_file);
    if (v_beamforming_config_file_val) {
        v_beamforming_config_file = VAL_STRING(v_beamforming_config_file_val);
    }

    v_band_number_val = val_find_child(
        inputval,
        y_o_ran_beamforming_M_o_ran_beamforming,
        y_o_ran_beamforming_N_band_number);
    if (v_band_number_val) {
        v_band_number = VAL_STRING(v_band_number_val);
    }

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if methnode is used */
    (void)methnode;

    /* invoke your device instrumentation code here */
    
    return res;

} /* u_o_ran_beamforming_activate_beamforming_config_invoke */
#endif /* u_o_ran_beamforming_F_MODIFY_BF_CONFIG */

/********************************************************************
* FUNCTION u_o_ran_beamforming_beamforming_information_update_send
*
* Send a u_o_ran_beamforming_beamforming_information_update notification
* Called by your code when notification event occurs
*
********************************************************************/
void u_o_ran_beamforming_beamforming_information_update_send (
    const xmlChar *v_band_number)
{
    val_value_t *parmval = NULL;
    status_t res = NO_ERR;

    if (!agt_notifications_enabled()) {
        log_debug2("\nSkipping <beamforming-information-update> notification; disabled");
        return;
    }
    
    if (LOGDEBUG) {
        log_debug("\nGenerating <beamforming-information-update> notification");
    }
    
    agt_not_msg_t *notif = agt_not_new_notification(beamforming_information_update_obj);
    if (notif == NULL) {
        log_error("\nError: malloc failed, cannot send "
        "<beamforming-information-update> notification");
        return;
    }
    
    /* add band_number to payload */
    parmval = agt_make_leaf2(
        beamforming_information_update_obj,
        y_o_ran_beamforming_M_o_ran_beamforming,
        y_o_ran_beamforming_N_band_number,
        v_band_number,
        &res);
    if (parmval == NULL) {
        log_error(
            "\nError: make leaf failed (%s), cannot send "
            "<beamforming-information-update> notification",
            get_error_string(res));
    } else {
        agt_not_add_to_payload(notif, parmval);
    }
    
    sil_sa_queue_notification(notif);
    
} /* u_o_ran_beamforming_beamforming_information_update_send */

/********************************************************************
* FUNCTION u_o_ran_beamforming_init
*
* initialize the o-ran-beamforming server instrumentation library
*
* INPUTS:
*    modname == requested module name
*    revision == requested version (NULL for any)
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_init (
    const xmlChar *modname,
    const xmlChar *revision)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    status_t res = NO_ERR;
    ncx_module_t *o_ran_beamforming_mod = NULL;

    o_ran_beamforming_mod = ncx_find_module(modname, revision);
    if (o_ran_beamforming_mod == NULL) {
        return ERR_NCX_OPERATION_FAILED;
    }

    beamforming_information_update_obj = ncx_find_object(
        o_ran_beamforming_mod,
        y_o_ran_beamforming_N_beamforming_information_update);
    if (beamforming_information_update_obj == NULL) {
        return ERR_NCX_DEF_NOT_FOUND;
    }

    /* put your module initialization code here */
    res = i_o_ran_beamformaing_init();

    return res;

} /* u_o_ran_beamforming_init */

/********************************************************************
* FUNCTION u_o_ran_beamforming_init2
*
* SIL init phase 2: non-config data structures
* Called after running config is loaded
*
* RETURNS:
*     error status
********************************************************************/
status_t u_o_ran_beamforming_init2 (void)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    status_t res = NO_ERR;

    /* put your init2 code here */

    return res;

} /* u_o_ran_beamforming_init2 */

/********************************************************************
* FUNCTION u_o_ran_beamforming_cleanup
*    cleanup the server instrumentation library
*
********************************************************************/
void u_o_ran_beamforming_cleanup (void)
{
    if (LOGINFO) {
        log_info("¥nEnter %s", __func__);
    }

    /* put your cleanup code here */
    
} /* u_o_ran_beamforming_cleanup */

/* END SIL-SA u_o_ran_beamforming.c */
